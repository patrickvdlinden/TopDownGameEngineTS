module SpritesheetBuilder {
    export enum PropertyFieldTypes {
        Text = "text",
        Number = "number",
        Boolean = "boolean",
        Select = "select",
        ColorPicker = "colorPicker"
    }
}